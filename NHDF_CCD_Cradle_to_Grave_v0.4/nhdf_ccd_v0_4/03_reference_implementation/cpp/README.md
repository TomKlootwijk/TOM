# C++17 reference slice

This directory mirrors the exact linear sphere-sphere query in a small dependency-free C++ library. It is intentionally narrower than the Python reference. Its purpose is ABI and build-system scaffolding, not performance claims.
