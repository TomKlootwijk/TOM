# CUDA skeleton status

The kernel is source-only and unverified in this package because the build environment had no CUDA compiler or target GPU. It must be tested against the Python/C++ reference before any performance statement is made.
