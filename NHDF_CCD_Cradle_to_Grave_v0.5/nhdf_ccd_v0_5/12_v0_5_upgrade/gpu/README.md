# GPU status

`feature_ccd_kernels.cu` contains only the embarrassingly parallel cubic-coefficient stage. It is a translation skeleton, not a completed backend. The release environment does not claim a CUDA compiler or GPU execution. A future backend must preserve status distinctions, TOI intervals, witness tolerances, stable ordering, capacity failures, and certificate digests under a declared numeric equivalence policy.
