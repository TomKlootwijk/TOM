# Preserved v0.4 baseline

The prior v0.4 PDF and ZIP are preserved byte-for-byte as the release baseline. v0.5 adds a separate upgrade directory and a cumulative dossier; it does not retroactively rewrite the prior evidence.
